prototype(GSL.DuttweilerDe:MultipleImages) < prototype(TYPO3.Neos.NodeTypes:AssetList) {
	fancyboxGroup = ${q(node).property('fancyboxGroup')}
	maxHeight = ${q(node).property('displayHeight')}
	maxWidth = ${q(node).property('displayWidth')}
}
